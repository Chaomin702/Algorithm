#include <iostream>
#include <vector>

using namespace std;

void shellSort(vector<int> &A){
	for (int gap = A.size() / 2; gap > 0; gap = gap / 2){
		for (int k = gap + 1; k < A.size(); k++){

		}
	}
}

int main(void){

	return 0;
}